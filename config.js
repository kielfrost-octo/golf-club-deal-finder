// Will be templated per environment by Octopus Deploy once that's wired up.
// For now this is committed directly so the live GitHub Pages site works.
export const WORKER_URL = 'https://golf-club-finder.kiel-frost.workers.dev';
export const UI_VARIANT = 'compact'; // 'compact' | 'grid' — OctoToggle-controlled
